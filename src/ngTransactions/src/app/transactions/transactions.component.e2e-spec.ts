//import { browser, element, by } from 'protractor';

describe('Transactions', () => {
    
    //beforeEach(async () => {
    //    return await browser.get('/');
    //});

    //it('should have correct feature heading', async () => {        
    //    expect(await element(by.css('h1')).getText()).toEqual('Spend');
    //});

});